rootProject.buildFileName = "build.gradle.kts"
rootProject.name = "gradle-plugin"

pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
        mavenLocal()
    }
}
